# =============================================================================
# ROS 2 / colcon build artifacts  — regenerated by `colcon build`, never commit
# =============================================================================
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/
# colcon may also drop these at any nested level:
**/build/
**/install/
**/log/

# =============================================================================
# Python virtual environments  — recreate from requirements.txt (see README)
# =============================================================================
.yolo_moveit/
.venv/
.yolo_venv/
venv/
env/

# =============================================================================
# Python caches
# =============================================================================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# =============================================================================
# Model weights
#   - base YOLO models (yolov8n.pt, yolo11n.pt, ...) are auto-downloaded by
#     ultralytics on first run, so we do NOT version them.
#   - the custom-trained cup model IS committed (see negation below) because a
#     junior cannot regenerate it without the training data.
# =============================================================================
*.pt
*.onnx
*.engine
!ros2_ws/yolo26_cup.pt

# =============================================================================
# Debug / generated artifacts
# =============================================================================
ros2_ws/frames_*.gv
ros2_ws/frames_*.pdf
*.rosbag
*.db3

# =============================================================================
# Editor / OS / tooling
# =============================================================================
.claude/
.vscode/
.idea/
*.swp
*~
.DS_Store
